{
  "name": "jupyterlab-datatable-renderer",
  "remoteUser": "jupyter",
  "forwardPorts": [8888, 9999, 9000, 9001],
  "portsAttributes": {
    "8888": {
      "label": "Jupyter Lab",
      "onAutoForward": "openBrowser",
      "protocol": "http",
      "url": "http://localhost:8888/lab"
    },
    "9001": {
      "label": "MinIO Console",
      "onAutoForward": "notify"
    }
  },
  "customizations": {
    "vscode": {
      "openExternal": true,
      "defaultUrl": "/lab",
      "extensions": [
        "ms-python.python",
        "ms-vscode.live-server",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "terminal.integrated.profiles.linux": {
          "jupyter bash": {
            "path": "/bin/bash"
          }
        },
        "terminal.integrated.defaultProfile.linux": "jupyter bash",
        "debug.node.autoAttach": "disabled"
      }
    }
  },
  "dockerComposeFile": "./docker-compose.yaml",
  "service": "jupyter-minio",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "mounts": [
    "source=${localEnv:HOME}${localEnv:USERPROFILE}/.ssh,target=/home/jupyter/.ssh,type=bind,consistency=cached"
  ],
  "postCreateCommand": "pip install -e . && jupyter labextension develop . --overwrite && jlpm build"
}
